// import express from "express";
// import * as appointmentController from "../../controllers/appointmentController.js";

// const router = express.Router();

// // Base path: /api/expert (mounted in server.js)

// // Get all appointments
// router.get("/:userId/appointments", appointmentController.getAppointments);

// // Create new appointment
// router.post("/:userId/appointments", appointmentController.createAppointment);

// // Update appointment
// router.put("/:userId/appointments/:appointmentId", appointmentController.updateAppointment);

// // Delete appointment
// router.delete("/:userId/appointments/:appointmentId", appointmentController.deleteAppointment);

// export default router;
