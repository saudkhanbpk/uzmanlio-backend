# Database
MONGO_URL=mongodb://localhost:27017/uzmanlio

# Server
PORT=4000
NODE_ENV=development
BASE_URL=http://localhost:4000

# Google Calendar OAuth2
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:4000/api/calendar/auth/google/callback
GOOGLE_WEBHOOK_TOKEN=your_secure_webhook_token_here

# Microsoft Calendar OAuth2
MICROSOFT_CLIENT_ID=your_microsoft_client_id_here
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here
MICROSOFT_REDIRECT_URI=http://localhost:4000/api/calendar/auth/microsoft/callback
MICROSOFT_WEBHOOK_SECRET=your_microsoft_webhook_secret_here

# Calendar Integration
CALENDAR_ENCRYPTION_KEY=your_32_character_encryption_key_here_change_in_production

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the .env file to version control
#
# Google Calendar Setup:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select existing one
# 3. Enable Google Calendar API
# 4. Create OAuth2 credentials (Web application)
# 5. Add your redirect URI to authorized redirect URIs
# 6. Copy Client ID and Client Secret
#
# Microsoft Calendar Setup:
# 1. Go to Azure Portal (https://portal.azure.com/)
# 2. Navigate to Azure Active Directory > App registrations
# 3. Create a new registration
# 4. Add redirect URI under Authentication
# 5. Add required API permissions (Calendars.ReadWrite, User.Read, offline_access)
# 6. Create a client secret under Certificates & secrets
# 7. Copy Application (client) ID and client secret value
#
# Webhook Setup:
# 1. Ensure your server is accessible from the internet (use ngrok for development)
# 2. Update BASE_URL to your public URL
# 3. Configure webhook endpoints in Google/Microsoft developer consoles
# 4. Generate secure random tokens for webhook verification
